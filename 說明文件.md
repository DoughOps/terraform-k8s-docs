```
根據你已知的 OpenStack 資源規格，請修改 Terraform 程式碼，目標是建立一個 Kubernetes L4LB 實驗環境的基礎網路拓樸。請建立以下資源：

1. 一個名為 k8s-net 的 private network，並建立 10.10.0.0/24 的 subnet（DHCP 可開啟或依你判斷）。
2. 建立2台 L4LB VM，名稱分別為 l4-0001、l4-0002，所有 NIC 接到 k8s-net，僅需基本 VM 建立，不用設定任何內部軟體，VM需要手動指定IP。
3. 建立3台 control plane VM，名稱分別為 ms-0001、ms-0002、ms-0003，所有 NIC 接到 k8s-net，僅需基本 VM 建立，不用設定任何內部軟體，VM需要手動指定IP。
4. 建立2台 worker VM，名稱分別為 ap-0001、ap-0002，所有 NIC 接到 k8s-net，僅需基本 VM 建立，不用設定任何內部軟體，VM需要手動指定IP。
5. 建立1台 router VM（可自行命名），同樣接到 k8s-net。
6. 建立一個 Floating IP，並綁定到 router VM，使其成為之後外部流量的入口節點。


此階段不需處理 FRR、BGP、VIP、Cilium 或任何 VM 內設定，僅需產生對應的 OpenStack Terraform 組態檔，用於建立上述網路與 VM 資源。
```